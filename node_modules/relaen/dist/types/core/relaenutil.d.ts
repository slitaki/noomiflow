declare class RelaenUtil {
    /**
     * 对象id计数器
     */
    private static idGenerator;
    /**
     * 生成对象id，提供 entitymanager 和connection使用
     */
    static genId(): number;
    /**
     * 处理字段字符串值
     * @param value     待处理值
     * @returns         处理后的字符串
     */
    static valueToString(value: any): string;
    /**
     * 处理参数占位符
     * @param sql   sql串
     * @returns     修改后的sql
     */
    static handlePlaceholder(sql: string): string;
    /**
     * 字符串转正则表达式
     * @param str       源串
     * @param side      匹配的边 1 左边 2右边 3两边
     * @returns
     */
    private static toReg;
}
export { RelaenUtil };

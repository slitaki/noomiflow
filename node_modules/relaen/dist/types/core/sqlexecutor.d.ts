import { EntityManager } from "./entitymanager";
/**
 * sql执行器
 */
export declare class SqlExecutor {
    /**
     * 执行mysql sql语句
     * @param em            EntityManager
     * @param sql           待执行sql
     * @param params        参数数组
     * @param start         开始记录行
     * @param limit         最大记录行
     * @returns             执行结果或undefined
     */
    static exec(em: EntityManager, sql: string, params?: any[] | object, start?: number, limit?: number): Promise<any>;
}

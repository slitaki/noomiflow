import { Transaction } from "./transaction";
/**
 * 事务管理器
 * @since 0.3.0
 */
export declare class TransactionManager {
    /**
     * 事务集合
     * key threadId,value 事务对象
     */
    private static transactions;
    /**
     * 添加事务
     * @param tx     transaction实例
     */
    static add(tx: Transaction): void;
    /**
     * 获取事务
     * @param threadId  thread id，默认为当前thread id
     * @returns         transaction实例 或 undefined
     */
    static get(threadId?: number): any;
    /**
     * 删除事务
     * @param threadId  thread id，默认为当前thread id
     */
    static remove(threadId?: number): void;
}

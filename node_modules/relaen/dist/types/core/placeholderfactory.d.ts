/**
 * 占位符工厂
 */
export declare class PlaceholderFactory {
    /**
     * 占位符map
     * key: 数据库dialect
     * value:{ch:占位符,start:开始索引号(有的开始于0，有的开始于1)}
     */
    private static map;
    /**
     * 添加placeholder
     * @param dialect           数据库产品
     * @param placeholder       占位符
     * @param startIndex        开始索引号
     */
    static add(dialect: string, placeholder: string, startIndex?: number): void;
    /**
     * 获取占位符
     * @param index     占位符索引，从0开始
     * @returns         占位符+索引号
     */
    static get(index: number): string;
}
